spring:
  jpa:
    hibernate:
      ddl-auto: none
  datasource:
    url: ${DB_URL:jdbc:mariadb://65.21.188.158:3306/xpress_dinero}
    username: ${DB_USERNAME:xpress_admin}
    password: ${DB_PASSWORD:Xpr3ss@2024!Secure}
    driver-class-name: org.mariadb.jdbc.Driver
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb+srv://reports_xpress_user_rw:ULh0VykDvetWM3JM@cluster0.zoa5e.gcp.mongodb.net/ReportsXpress}

sentry:
  dsn: ${SENTRY_DSN:https://91bb4322f0905e42c4acc5b9f3873975@o1037894.ingest.us.sentry.io/4508762622459904}
  traces-sample-rate: ${SENTRY_TRACES_SAMPLE_RATE:1.0}

# Configuración de Spring Boot Actuator para producción
management:
  endpoints:
    web:
      exposure:
        include: health,info,loggers,logfile,metrics,httpexchanges
      base-path: /actuator
  endpoint:
    health:
      show-details: never
    loggers:
      enabled: true
  security:
    enabled: false

# Configuración de logging para producción
logging:
  file:
    name: ${LOGGING_FILE_NAME:/app/logs/sfast-xpress-prod.log}
  level:
    root: ${LOGGING_LEVEL_ROOT:INFO}
    tech.calaverita.sfast_xpress: ${LOGGING_LEVEL_TECH_CALAVERITA:INFO}
    org.springframework: WARN
    org.hibernate: WARN
    org.springframework.security: WARN